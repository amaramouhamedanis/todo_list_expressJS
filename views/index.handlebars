<h1>📌 Listes des Tâches</h1>

<div class="list-group">
  {{#each taches}}
  <div class="list-group-item {{#if @first}}active text-white{{/if}}">
    <div class="d-flex w-100 justify-content-between">
      <h5 class="mb-1">{{this.title}}</h5>
      <small class="{{#if @first}}text-white{{else}}text-muted{{/if}}">3 days ago</small>
    </div>
    <p class="mb-1">Description de la tâche ici...</p>
    <small class="{{#if @first}}text-white{{else}}text-muted{{/if}}">Détails supplémentaires...</small>

    <!-- Boutons Modifier et Supprimer -->
    <div class="mt-2">
      <a href="/modifier/{{this.id}}" class="btn btn-warning btn-sm">Modifier</a>

      <form action="/supprimer/{{this.id}}" method="POST" style="display:inline;">
        <button type="submit" class="btn btn-danger btn-sm">Supprimer</button>
      </form>
    </div>
  </div>
  {{/each}}
</div>

<!-- Bouton pour ajouter une tâche -->
<div class="mt-3">
  <a href="/ajouter" class="btn btn-primary">Ajouter une tâche</a>
</div>
